{% extends 'base.html.twig' %}

{% block body %}
{{ form_start(form) }}
    {{ form_row(form.method) }}
    {{ form_row(form.responseStatus) }}

    {{ form_widget(form.headers.vars.prototype) }}
    {# <ul class="headers" data-prototype="{{ form_widget(form.headers.vars.prototype)|e }}">
         {{ form_widget(form.headers.vars.prototype)|e }}
     </ul>#}

    {{ form_row(form.body) }}
{{ form_end(form) }}
{% endblock %}